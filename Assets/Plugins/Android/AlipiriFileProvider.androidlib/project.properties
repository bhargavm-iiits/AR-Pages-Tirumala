# This file marks this folder as an Android Library for Unity's Gradle export —
# the ".androidlib" folder-name suffix plus this file is what Unity's build
# pipeline looks for (classic Eclipse ADT convention, still honoured by Unity's
# Gradle template generation).
android.library=true
